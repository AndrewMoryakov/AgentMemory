{
  "mcpServers": {
    "agentmemory": {
      "type": "stdio",
      "command": "<SHELL_COMMAND>",
      "args": [
        "<ABSOLUTE_PATH_TO_AGENTMEMORY>/scripts/run-agentmemory-mcp.<ps1-or-sh>"
      ],
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}"
      }
    }
  }
}

